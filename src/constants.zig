// [3] c-byte-order-mark from the spec (section 5.2)
pub const C_BYTE_ORDER_MARK = 0xFEFF;
// [33] s-white from the spec (section 5.5)
pub const S_WHITE = " \t";

pub const MAX_SUPPORTED_YAML_MAJOR_VERSION = 1;
pub const MAX_SUPPORTED_YAML_MINOR_VERSION = 2;